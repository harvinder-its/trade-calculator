# Styles

Global styles live in `app/globals.css`. Tailwind configuration is in `tailwind.config.ts` at the project root.

Custom utility classes and CSS variables are defined in the globals file for theme-aware colors and dark mode support.
